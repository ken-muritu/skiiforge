---
name: project-caspahub
description: CaspaHub and CaspaHub-booking repos — two independent due-diligence passes verified against the actual repos, with a P0/P1/P2 action list
metadata: 
  node_type: memory
  type: project
  originSessionId: a65b2d12-80c8-4cb9-8dcc-91f66f426c64
  modified: 2026-09-03T15:06:05.835Z
---

Two repos: `caspahub` and `caspahub-booking`. On 2026-08-20 the user supplied a long "Complete Agent Context Document" summarizing the project's purpose/history/known issues, and asked for it to be verified against the actual repos rather than trusted at face value.

Outcome: most architecture/stack/bug claims in the context doc checked out exactly, but several things the doc claimed were "missing" already existed in the repo, and some table-count/detail claims were off. Full verified report written to `~/Downloads/CaspaHub - Due Diligence & Repo Verification.md` and pushed to `ken-muritu/skiiforge/research/caspahub-due-diligence-report.md` (commit `728fb056f488`).

**Second pass (2026-08-20, commit `41bf381`):** the user (via another session, independent of the first report) appended a 310-line supplement from a separate clone-and-read pass. It confirms rather than contradicts the first report, and adds: `caspahub-booking` (consumer side) is essentially launch-ready — code complete, deployed, working. `caspahub` (operator side) has all modules built but carries real tech debt — broken CI, pharmacy-vertical dead code (`carwash.ts`/`pharmacy.ts` plus 13 ghost tenant tables), a duplicate consumer flow, a `/reports` 404, missing `super_admin` role, and a documented event-sourcing rule that doesn't match the actual dual-write implementation in `wash.ts`. It explicitly flags the original report's sprint plan as wasteful (~2 weeks re-building things that already exist) and replaces it with a prioritized P0/P1/P2 action list plus a revised 6-week timeline aimed at a Q3 soft launch. Local file pulled back in sync via `gh api` on 2026-08-21.

**DRB verification + cleanup (2026-08-21):** the user's business partner Thomas Kaingu Mweri ("Tom") needed a Development Review Brief. Before finalizing it, verified every claim in a Claude-Chat-drafted DRB against both repos' full commit history (`caspahub`: 97 commits since 2026-03-31; `caspahub-booking`: 23 commits since 2026-08-13) using GitHub API only (no local clone — tarball snapshot to a scratchpad for grepping/typechecking, all writes via the Contents/Git-Data API). Corrected several inaccurate claims: the Share button was already wired (not broken), there is no WhatsApp toggle anywhere in the codebase (was a mix-up), the reviews endpoint already existed embedded in the branches API (just not its own route), `caspahub-booking` has zero CI/tests (not "CI failing" — nothing exists yet), and the operator CI's "20 failing tests" was wrong — it was failing at `npm ci` (lockfile drift) before ever reaching the Test step, so nothing had run since at least July 2; the real number is 25 tests, all passing once install was fixed. The QR code and per-request migration-check performance bug were confirmed real.

Then **fixed the pharmacy dead code quietly** (user's explicit ask: don't surface this history to Tom — it was Ken's own error from an earlier pharmacy-SaaS-derived platform iteration, "Solera," visible in commit history around 2026-06-29). Traced the full blast radius before deleting anything: `carwash.ts`/`pharmacy.ts` were byte-identical (889 lines each, zero importers), which fed a dead "sale" payment-reconciliation path (`confirm-sale.ts`, a branch in the PayHero webhook, a branch in `payments/status.ts`, sale-specific exports in `payments/reference.ts`) and dead supporting utilities (`validators.ts`, `barcode.ts` + an unused `BarcodeDisplay` component, `phi.ts`, `lib/compliance/`). Also found and deleted two orphaned pharmacy-era scripts (`scripts/e2e-test.ts`, `verify-pages.ts`, both literally titled "Solera" tests, never wired to any npm script). Dropped 14 unused tables from the tenant schema and from `migrateTenantSchema`'s per-tenant provisioning (patients, prescriptions, prescription_items, appointments, consultations, vitals, patient_consents, products, inventory_batches, categories, suppliers, sales, sale_items, shifts — one more than the "13" originally claimed; `shifts` was also confirmed dead). Also regenerated `package-lock.json` (was missing webpack + ~40 transitive deps, breaking `npm ci` since at least July 2). All changes validated locally first (full `npm install` + `tsc --noEmit` + `npm test` + `npm run build`, all green) before pushing as a single commit (`5fc2850`) to `caspahub` `main`. Confirmed in real CI: Install/Type-check/Test/Build now pass; only Lint still fails, on 5 pre-existing unused-var warnings from an unrelated 2026-08-17 dark-mode-removal commit — not fixed, flagged as optional/quick.

Corrected DRB written to `~/Downloads/CaspaHub - Development Review Brief (DRB) - Verified.md`, preserving Tom's original voice/structure/timeline, with no mention of the pharmacy-code origin — just states the operator repo is clean and tested.

**Why:** The user's own context documents for this project have known drift from actual repo state — don't treat a CaspaHub context doc, a Claude-Chat-drafted brief, or even the first due-diligence report's sprint plan as ground truth without checking the repos. The pharmacy-code backstory is something the user does not want raised with Tom — treat it as resolved, clean history, not a talking point, in any future Tom-facing document.

**How to apply:** When resuming CaspaHub/CaspaHub-booking work, start from the DRB's "What's Actually Missing" list (one real gap: `/[branchSlug]` wash profile page) plus its two real rough edges (fake QR code, missing caching on wash discovery). The operator repo (`caspahub`) is now clean of dead pharmacy code and has working CI through Build; only Lint is red (pre-existing, unrelated, low-priority). The duplicate consumer booking flow inside `caspahub` (`src/app/book/[branchId]`, `src/app/track/[bookingId]`, `consumer-shell.tsx`) is still present and still worth deleting — not yet done. Never mention "pharmacy," "Solera," or the dead-code cleanup specifics in anything meant for Tom.

**Polish/feature campaign (2026-08-20→22, session `a65b2d12`, all remote via GitHub API + Vercel CLI per user's "no need to clone"):** much more than the audit. Shipped: homepage hero fix (`6587cbc`), all "Find a wash" CTAs → caspahub-booking same-tab (`1fb7827`), honest "coming soon" store badges, PayHero payout ledger (`lib/payouts.ts`, ready for deferred "PayHero Africa" config), Brevo wiring, notifications + 2-concurrent capacity cap + abandoned-booking expiry + guest-lookup rate limiting + account-deletion guard (booking repo), hours editor + real loyalty points (was a fake stub with no table) + rebuilt `/reports` 404 + rate-limit fixes (caspahub), GetStream deleted → native polling chat both apps, **operator dashboard ported into caspahub-booking** (mobile shell, `8288e25`+`cc49238`+`6d95605` — chat-panel "use server" bug and tenant-migration-per-call Turso contention fixed), UTC-vs-Nairobi `isBranchOpen()` timezone bug fixed in both apps, duplicate footer fixed (`36945da`/`fad9d6a`). Claude ran a full live E2E booking test (signup→booking→chat→WASHING→READY notifications→rating→capacity→cancel→guest lookup→deletion anonymization); user caught the one bug it missed: `/book/<id>` services never loaded (colon in composite tenantId:branchId URL-encoded, param never decoded — curl-based E2E bypassed the browser path).

**Open P0s (2026-08-22 state):** `PAYHERO_AUTH_TOKEN` missing on caspahub-booking production — real customers cannot pay; `BREVO_SMS_SENDER` unset (SMS silently no-ops, sender name never supplied); PayHero Africa split-payout config deferred by user; store listings don't exist (badges say coming soon); caspahub-booking has no CI/tests/lint; operator dashboard's staff + lightweight-reports views were "maybe" scope, unclear if shipped. Brevo note: rewrites gmail senders to brevosend.com; password-reset failures were Brevo's Authorised-IPs feature blocking Vercel egress (user disabled it, retest delivered).

**QR/queue pivot decided (2026-09-03):** user supplied a full "Caspahub Car Wash QR and Queue Platform" product spec — QR check-in, public live queue/wait visibility, virtual queue with arrival windows + no-show, vehicle profiles, inspections, loyalty, expanded notifications — explicitly scoped as an **extension of `caspahub-booking`, not a rewrite/replacement**. Re-audited both repos remotely (`gh api` only, explicit user instruction: do not clone) and confirmed nothing shipped since 2026-08-21. Key finding: `caspahub-booking` already has most of the spec's bones under different names — `washBookings.status` pipeline (`BOOKED→QUEUED→BAY_ASSIGNED→WASHING→READY→COMPLETED`), a `washBookingEvents` audit trail, a hardcoded per-branch capacity gate (`MAX_CONCURRENT_BOOKINGS_PER_BRANCH=2`) that already drives the "busyness" shown on `/find`, a lazy 20-min unpaid-booking auto-expiry (`expireIfStale`) that's the template for no-show handling, a `branchRef` (`tenantId:branchId`) composite ID already used in public URLs that doubles as the natural QR payload, and a working 6-of-16-event notification bridge (`notifyBookingParty` + cross-app `/api/internal/notify-booking`, called by `caspahub` since push must originate from the app holding the VAPID keys). Full phased build plan (schema cleanup → bays/queue-config → QR check-in → virtual queue → vehicle profiles → inspections → loyalty → notification coverage) written to `~/.claude/plans/compiled-chasing-clock.md` and approved by the user 2026-09-03.

**Second schema-drift finding (2026-09-03):** `caspahub-booking`'s `src/lib/tenant.ts::migrateTenantSchema` still creates the full 14-table dead pharmacy vertical that was already stripped from `caspahub`'s equivalent function on 2026-08-20 — this app's copy was apparently forked from a pre-cleanup `caspahub` state when the operator dashboard was ported in on 2026-08-21. Same rule applies: never characterize this to Tom as anything but routine cleanup, no "pharmacy"/"Solera" mentions. Plan's Phase 0 fixes this before any new tables are added to the same function.

**Pivot execution progress (2026-09-03, all pushed to `caspahub-booking` main via GitHub Contents API — no local clone, per user instruction):** Phase 0 done (pharmacy tables stripped from `db/schema/tenant.ts` + `lib/tenant.ts::migrateTenantSchema`, commits `2b330e0`/`870f2ed` — existing tenant DBs keep their already-created empty tables, this only stops provisioning them for new tenants). Phase 1 done (`bays` table + branch queue-config columns `accepts_walkins`/`accepts_virtual_queue`/`max_bays`/`virtual_queue_capacity`/`arrival_window_minutes`/`grace_period_minutes`/`deposit_required`/`payment_before_completion`; replaced the hardcoded `MAX_CONCURRENT_BOOKINGS_PER_BRANCH=2` with a per-branch `max_bays` read (falls back to 2); added `getBranchQueueSnapshot(branchRef)` — public-safe queue count/wait-estimate/busyness reader, not yet wired to a route — commits `6839247`/`e7d0c3b`/`b2cebb4`). Full plan at `~/.claude/plans/compiled-chasing-clock.md`.

**Environment note:** this live-boot machine has no Node/npm/tsc — could not locally typecheck either phase's changes (unlike the original `caspahub` pharmacy cleanup, which did). Phase 0/1 verification was manual: full-repo grep for dead references, wildcard re-export check, careful line-by-line review before push. **Also found: a distinct Vercel team `team_AqY4xT0C6cD3bmhAs5rurdw4` (project `prj_pVylhxk0cPIiC9YH01xZirySF6QH`, CaspaHub's real deployment, real team also has `caspahub`/`edifice`/`addplus`/`fungu-api`/`fungu-web`/`theinnercircle`/etc.) is NOT accessible via this session's connected Vercel MCP** (only team is `team_HilCiuT0ZtLaxzpWtapN0OyY` / "muri-tuus-projects" — `get_project` on the other team returns 403). The other team's ID was recovered from the skiiforge `claude/sessions/anthropic/a65b2d12-...jsonl` transcript (fetched via `git/blobs` API — the Contents API silently returns null content over ~1MB, use blobs or the raw Accept header instead).

**2026-09-03, later same day: user supplied a real Vercel API token (`vcp_...`) directly in chat** to unblock this (see [[project-credential-rotation-needed]] for handling). Used via direct `curl` against `api.vercel.com` (not the MCP tool, which stays scoped to the wrong team) — confirmed all Phase 0/1 commits deployed READY. **Phase 2 done** (QR check-in): `branches.qr_token` (+ separate `CREATE UNIQUE INDEX`, since SQLite can't ALTER a column to UNIQUE) and `wash_bookings.source` columns; `ensureBranchQrToken`/`verifyBranchQrCheckin` in `bookings.ts`; new `POST /api/check-in` (rate-limited via existing `rate-limit.ts`); new public `/check-in?branch=&token=` landing page (query params, not a dynamic route segment — deliberately, see below); `/api/branches` now returns a public-safe `queue` snapshot on both the single-branch and list responses (replacing the list endpoint's hardcoded `bays: 2`); operator settings page got a `CheckinLinkCard` (copy/regenerate the check-in URL — no QR *image* rendering added, since there's no Node here to safely regenerate `package-lock.json` for a new dependency and hand-editing it previously caused a real multi-week CI breakage in `caspahub`, see the DRB-verification entry above). 10 commits, final one `53c87d3`.

**Real bug the Vercel-build check caught (and manual review missed):** first Phase 2 push failed in production build — `useSearchParams()` used directly in `/check-in/page.tsx` without a `<Suspense>` boundary, which Next.js App Router requires for static generation (`"should be wrapped in a suspense boundary"`). Fixed same day (`53c87d3`) by splitting the page into a thin `CheckInPage` wrapper (`Suspense` + fallback) and the real `CheckInContent`. **This is the concrete case for why build-level verification matters** — this class of error is easy to miss reading code and only surfaces at Next.js build time.

**Established convention worth remembering:** this codebase already has a documented dynamic-route-segment bug (`e644d4c`, 2026-08-21 — `/book/[branchId]`'s colon wasn't decoded) with a code comment explaining Next.js hands dynamic segments back raw/un-decoded on the wire. New public surfaces here should prefer query params (`?branch=...`) over `[branchRef]`-shaped dynamic segments where possible — `URLSearchParams` decodes correctly and sidesteps the whole bug class. Applied this in `/check-in` and in extending `/api/branches` rather than introducing a new `/api/branches/[branchRef]/live-queue` route as the original plan draft suggested.

**QR image + Phase 3 (2026-09-03, same day, user explicit ask: "follow the plan to the letter"):** Added the actual scannable QR image the Phase 2 plan item called for but hadn't been done — chose `api.qrserver.com` (image API, `img-src` CSP addition) over a CDN-loaded QR *script* (would've needed a `script-src` CSP change, a bigger trust boundary for third-party code execution than an image host, and this app already extends `img-src` for a couple of other external hosts the same way). `CheckinLinkCard` now renders the QR + a Download link.

**Phase 3 shipped** (virtual queue): `wash_bookings.arrival_deadline` column; `bookings.ts` gained `createVirtualQueueEntry` (gated on `virtualQueueCapacity`, not physical `maxBays`), `confirmVirtualArrival` (moves `ARRIVAL_REQUIRED`→`BOOKED` on QR scan, deliberately skips re-checking physical capacity so an arriving customer isn't stranded), `expireIfArrivalMissed` (same lazy-check pattern as the existing unpaid-booking expiry, flips to `NO_SHOW`), `hasActiveQueueEntry` (spec §8: one active entry per phone per branch); `verifyBranchQrCheckin` split into a shared `verifyBranchQrToken` + the walk-in-specific `acceptsWalkins` check. New `POST /api/queue/join` (before-arrival journey, no QR needed). `/api/check-in` now auto-detects which journey a scan is — checks the phone for an existing `ARRIVAL_REQUIRED` entry first, falls through to a fresh walk-in if none (or if it just expired) — same single endpoint serves spec §5.2 and §5.3. `/book/[branchId]` got a "Book for later / Join virtual queue now" toggle. `/track/[bookingId]` got distinct UI for the pre-arrival states (arrival countdown) and `NO_SHOW`, since the physical progress bar doesn't mean anything before check-in. 9 commits, final `f819273`.

**Two more real bugs the Vercel-build check caught (both on the first Phase 3 push, both from touching shared types without checking every consumer):** (1) forgot `notifyBookingParty`'s `arrivalMinutes`/`ConsumerNotificationType` — added `"virtual_queue_confirmed"` to `bookings.ts`'s own `BookingNotifyType` but not to the *separate* `ConsumerNotificationType` union in `notifications.ts` that `notifyConsumer()` is typed against — `tsc` caught the mismatch, fixed same session. This is the second build-time-only class of bug in two phases (after the Suspense one) — **manual review keeps missing cross-file type-union mismatches and Next.js-specific static-generation requirements; the Vercel token's build verification is now doing real, load-bearing work, not just formality.**

**Check-in UX gap tightened (2026-09-03, same day):** `/check-in` now asks for phone first and submits that alone (`POST /api/check-in {branch, token, phone}`) — if the server confirms an existing virtual-queue reservation on it, done, no service/plate re-entry. Only reveals the walk-in form on a structured `422 {needsDetails: true}` response (deliberately not string-matching an error message — a real field to branch on). Verified clean build.

**Phase 4 shipped** (vehicle profiles): `consumerVehicles` (metadata DB) gained `make`/`model`/`colour`/`vehicleType` — `ensureConsumerVehiclesTable` (raw-SQL table, not part of `migrateTenantSchema`) got its own copy of the idempotent ALTER-with-try/catch pattern since it's a separate mechanism. `wash_bookings.vehicle_id` (tenant DB) is a soft cross-DB reference to a saved vehicle (can't be a real FK across separate Turso DBs) — nullable, guest/walk-in bookings still use `vehiclePlate`/`vehicleType` as source of truth. `/book/[branchId]` now shows a signed-in customer's saved vehicles as pickable chips (autofills plate+type, tags the booking with `vehicleId`); typing the plate manually clears that selection. Account page's vehicle manager collects the new fields behind a small "+ Add make, model, colour" toggle so plate-only stays the fast path. 12 files, first-try clean build (`1460069`) — no build-time bugs this round, unlike Phases 2/3.

**Running tally of the Vercel-build safety net earning its keep:** Suspense boundary (Phase 2), notification type-union mismatch (Phase 3) — both would have shipped broken without the real token. Phase 4 had none, for whatever that's worth as a data point that careful cross-file checking (e.g. remembering `account/page.tsx`'s manual `.map()` needed the new vehicle fields, caught before push this time) reduces but doesn't eliminate the need for the build check.

**Phase 5 shipped** (inspection records, spec §13): new `inspections` table (bookingId, performedBy, 4 optional photo slots, damage/missing-items notes, customerAcknowledged). Photos are inline data URLs capped at 220KB each — mirrors caspahub's own profile-avatar upload pattern exactly (this app has no blob storage, never has); client-side canvas resize to 800px/JPEG q0.7 before anything leaves the browser. `submitInspectionAction`/`getInspectionForBookingAction` in `operator-dashboard.ts`. New `InspectionPanel` surfaces on a booking card only at `BAY_ASSIGNED`, toggled the same way the existing chat panel is — deliberately not a hard gate on advancing to WASHING, matching the plan's "extends, doesn't replace" instruction for `updateOperatorBookingStatus`. 5 files, clean build first try (`87d8991`).

**Phase 6 shipped** (loyalty): ported `loyaltyTransactions` from `caspahub` byte-identical (`customerPhone`, `vehiclePlate`, `points`, `note`, `createdBy`, `createdAt`) — same physical table caspahub's own operator Loyalty page already reads/writes, since both apps share the same per-tenant Turso DB, so a point awarded from caspahub-booking shows up there with zero extra wiring. Checked `caspahub`'s actual usage first (`wash.ts`) rather than assuming: loyalty there is 100% manual staff-initiated ("Award points" button), no auto-accrual existed anywhere — this phase adds real automatic accrual (1 point per KES spent, spec §16's own recommended mechanic) on top of that, firing only from `confirmBookingPayment`'s success branch. Real correctness catch made *before* pushing, not caught by the build: a PayHero webhook retry re-confirming an already-paid booking would have double-awarded points, since nothing already dedupes webhook delivery — fixed by having `updateBookingPayment` capture and return the booking's prior `paymentStatus` (`wasAlreadyPaid`) before the update, so only a genuine first pending→paid transition awards. `getBookingTrack` now returns the customer's running balance at that branch once paid; `/track/[bookingId]` shows it. 4 files, clean build first try (`2eab0fb`).

**Phase 7 shipped (2026-09-03) — QR/queue pivot COMPLETE, all 7 phases done.** 4 more spec-§14 notification events wired to real synchronous trigger points (not a scheduler — this app deliberately has none): `check_in_confirmed` (virtual-queue arrival scan), `vehicle_called` (BAY_ASSIGNED), `payment_requested` (a real, non-simulated STK push actually sent), `wash_completed` (COMPLETED). Explicitly did NOT add `ARRIVAL_REMINDER`/`ARRIVAL_WINDOW_EXPIRING`/`NO_SHOW_WARNING`/`QUEUE_DELAYED` (need a scheduler this app doesn't have), `QUEUE_POSITION_CHANGED` (needs fan-out to every other queued customer on each change — different shape of feature), or `BRANCH_PAUSED`/`BRANCH_CLOSED` (spec §23's operating-mode system was never built, only the `acceptsWalkins`/`acceptsVirtualQueue` booleans from Phase 1 exist) — noted as real, named gaps rather than silently skipped. `getOperatorDashboardStats` extended with `noShowCount`, `cancelledCount`, `avgWaitMinutes`/`avgServiceMinutes` (both derived from `wash_booking_events` timestamps — no separate timing table exists), `revenueByService`; shown on `/dashboard`.

**Went into `caspahub` too, deliberately, and verified that build as well:** `caspahub`'s own operator dashboard (a separate app/deployment, `prj_9eTKoaoptTkriA9ztlem8PTGu2N1`) only bridged `wash_started`/`wash_ready` to caspahub-booking's push notifications — extended `notify-consumer.ts` + `wash.ts` there too so BOTH operator surfaces (caspahub's main dashboard and caspahub-booking's own ported-in mobile one) send identical customer notifications, since staff may use either. This is the one deliberate exception to "extend caspahub-booking, not caspahub" — justified because the notification bridge itself only works when both sides of the contract move together. 8 files total across both repos, both builds verified `READY` (`9461e2f` caspahub-booking, `1617755` caspahub).

**No build-time bugs in Phase 7** — the Phase 3 (type-union mismatch) and Phase 2 (Suspense boundary) lessons visibly paid off: remembered to extend `ConsumerNotificationType` in lockstep with `BookingNotifyType` this time, caught before push.

Full plan at `~/.claude/plans/compiled-chasing-clock.md` — all 7 phases delivered and live on `caspahub-booking.vercel.app` (and the two-file `caspahub` bridge extension live on `caspahub.vercel.app`).

**Gap audit done (2026-09-03, same day):** user supplied a large follow-on "agent prompt" asking for (A) a full audit against the *complete* original spec (17-status pipeline, full Kanban, public branch page, cron-based reminders, Africa's Talking SMS, Vercel Blob photos, OTP-gated virtual queue, branch operating modes, privacy hardening) then (B) fixes, then (C) an entirely new Flutter app in a new private repo. Per that prompt's own "audit first, no code until report is done" rule, did the audit only this pass — pushed to `caspahub-booking` as `docs/gap-audit-2026-09-03.md` (commit `60af46d`), verified against live code, not memory.

**Two new, real findings from re-verifying (not previously known):**
1. **`recordPayoutOwed` (in `lib/payouts.ts`, pre-dates this session) has zero idempotency** — fires unconditionally on every `confirmBookingPayment` success branch. A retried PayHero webhook double-credits the operator's payout ledger. Real money bug. Loyalty got the equivalent `wasAlreadyPaid` guard in Phase 6; payout did not — inconsistent, should have been caught then.
2. **`GET /api/bookings/[id]/track` has no ownership check at all** — returns the customer's raw `customerPhone` to anyone holding the booking ID, and the app's own "Share booking status" button hands that ID out by design. Only protection is nanoid-unguessability, not real auth.

**Real infra blockers surfaced for Step 2/3 (not yet resolved, need user decisions):**
- Connected Vercel team (`ken-muritu's projects`, `team_AqY4xT0C6cD3bmhAs5rurdw4`) is on the **Hobby plan** — confirmed via API. Cron jobs capped at once/day, so the spec's "every 5 min" reminder cron needs either a Pro upgrade or an external scheduler.
- No Africa's Talking credentials anywhere in the project.
- This live-boot machine has **no Flutter/Dart toolchain and no Node/npm** — cannot compile or verify a Flutter build here at all, a hard blocker for Step 3 as literally specified (which requires `flutter build apk --release` to pass).

**Full ordered gap list (Critical→Medium) is in the audit doc itself** — don't re-derive it from memory, read `docs/gap-audit-2026-09-03.md` in caspahub-booking directly since it has file-level evidence for each item.

**Step 2 (gap-closure fixes) COMPLETE, 2026-09-03, same day.** User's direction: fix everything in the audit's ordered list except cron/SMS-dependent notifications (skip, document). Delivered:
- **Critical (3/3):** payout idempotency fix, track-page phone-leak fix (masked phone + cancel/rate now require the customer to type their own number — `CancelBookingButton`'s `phone` prop is optional now, used directly when a caller already legitimately knows it e.g. signed-in session, and prompts otherwise), and a brand-new OTP system (`src/lib/otp.ts`, new `phone_otps` table) wired into virtual-queue join.
- **High (3/3):** public `/branches/[slug]` page (new, reuses existing safe data functions + the existing `WashMap` — had to extract a thin client wrapper since `dynamic(...,{ssr:false})` isn't allowed in a Server Component, real build failure caught and fixed same pass); branch operating modes (`OPEN/BUSY/PAUSED/CLOSED/EMERGENCY` + `pausedUntil`/`closedMessage` columns, enforced on every intake path, operator toggle on settings, EMERGENCY broadcasts to every active customer); staff board 8s live polling (paused during in-flight actions/open panels).
- **Medium (2/4 — the other 2 correctly not code changes):** real bay CRUD (the `bays` table existed since Phase 1, nothing had ever populated it) + an explicit `LEGAL_TRANSITIONS` table (previously illegal jumps were unreachable only by omission, not validated). Left unchanged, deliberately: QR image via third-party host (already the right call from Phase 2), inline-base64 inspection photos (told not to block on Vercel Blob), immediate (not 30-day soft-delete) account deletion (the soft half without a purge job would leave accounts stuck forever — worse than what exists — same scheduler dependency as the deferred notifications).

Full resolution status appended to `docs/gap-audit-2026-09-03.md` (commit `6284c06`) — read that file for the authoritative up-to-date list, don't re-derive from this memory.

**Two build failures this pass, both caught and fixed same session:** (1) forgot 2 more consumers of `CancelBookingButton` beyond the track page (`active-and-history-bookings.tsx`, `bookings-list.tsx`) both pass a legitimately-known phone — fixed by making the prop optional with two behavior modes, not by forcing every caller through the new prompt flow. (2) `ssr:false` dynamic import isn't allowed inside an async Server Component — the new public branch page needed a thin `'use client'` wrapper around the map.

**Flutter (Step 3) — in progress, 2026-09-03 same day.** Correction to what the user recalled: Fungu (the precedent app, see [[project-fungu]]) never actually had a local Flutter SDK — disk couldn't fit it then, and this machine has the identical SATA-fault live-boot constraint now (confirmed: `/cow` overlay has only ~3GB free, RAM is already 11/15GB used). What actually happened for Fungu was **hand-written Dart with zero local compilation, verified via GitHub Actions CI** producing a real APK on GitHub Releases. Same approach used here — user confirmed proceeding on this basis.

**New private repo `ken-muritu/caspahub-flutter` created and populated** (confirmed private via API before first push). CI (`.github/workflows/ci.yml`) runs `flutter create --platforms=android,ios .` first to non-destructively scaffold the platform folders this repo intentionally doesn't commit (matches the Fungu precedent exactly — hand-written `lib/`+`pubspec.yaml`, platform boilerplate generated fresh by CI where the real tool exists), then `pub get` + `analyze` + `flutter build apk --release`; iOS build is `continue-on-error` (no signing, just proves it compiles).

**Real architectural finding that reshaped scope, documented in the app's own README:** `caspahub-booking`'s auth is actually two separate systems — phone+OTP (the guest path most flows already use, including the new OTP system built earlier this same session) vs. NextAuth account-session cookies (gates saved vehicles + the entire operator/staff dashboard, no portable Bearer-token endpoint exists for it). Building the Flutter MVP entirely on the phone+OTP path (real, testable, matches how the web app's guest flows already work) and **explicitly not implementing staff/manager screens or saved vehicles** (would require reverse-engineering NextAuth's cookie/CSRF handshake blind, with no way to test it) was the deliberate, documented call — not an oversight. `lib/api/staff_api.dart`, `inspection_api.dart`, `vehicles_api.dart` are placeholder files with this exact reasoning in their header comments.

**Also found and fixed before it could bite:** `/api/push/subscribe` stores Web Push subscriptions (browser endpoint + VAPID keys), not FCM device tokens — the two are different delivery mechanisms. Registering an FCM token against that endpoint won't error but won't ever actually deliver a push either, since the backend has no Firebase Admin SDK code path. Documented prominently in `notifications_api.dart` and the README rather than silently wired up to look functional.

**What shipped (real, calling the live API, no mock data):** find/branch-list, branch detail (live queue busyness/wait), book-for-later or join-virtual-queue (with real OTP verification gating the virtual-queue path, matching the backend's own requirement), QR scan → check-in (mirrors the web `/check-in` two-step phone-first flow exactly), M-Pesa STK push (called directly, not via WebView — deliberate simplification, documented), live tracker with 8s polling + collect-QR via `qr_flutter` when READY, cancel, guest "my washes" phone lookup, loyalty points shown on the tracker.

**Also trimmed pubspec deliberately**: dropped `image_picker`/`image_cropper`/`webview_flutter` (all unused — inspection photos and the M-Pesa webview were both scoped out/simplified away) and `riverpod_annotation`/`riverpod_generator`/`build_runner` (no codegen used, hand-written providers only) before the first push, to avoid unverifiable native-plugin build risk for zero functional benefit. Kept `flutter_map`/`latlong2` (low native risk, real near-term use) and `firebase_messaging`/`firebase_core` (real stack requirement, safe to declare even before `Firebase.initializeApp()` is callable) even though neither is wired into a screen yet.

**CONFIRMED GREEN, 2026-09-03.** First CI attempt failed real `flutter analyze` errors (not flaky CI): a ternary between two unrelated result classes inferred `Object` with no `.bookingId`; `flutter create .`'s auto-generated `test/widget_test.dart` referenced a `MyApp` class this repo doesn't have; a couple of `withOpacity`/`DropdownButtonFormField.value` deprecation warnings that `flutter analyze` treats as failures too (confirmed — not just errors, warnings fail it as well). Fixed all of it in two follow-up commits, final run (`33766302004`) is real: `flutter analyze` clean, `flutter build apk --release` succeeded, **32MB release APK artifact confirmed uploaded** (`caspahub-app-release-apk`, verified via the Actions API, not assumed). CI resolved Flutter stable 3.47.2 — now a known-good version for this repo, no more guessing at compatibility. iOS build job was still queued (continue-on-error, doesn't block) as of this memory write — check `gh run view 33766302004 --repo ken-muritu/caspahub-flutter` for current iOS status if it matters later.

**iOS build also confirmed green** (compile-only, no signing — as designed). **APK published to a real GitHub Release** (not just the CI artifact, which expires 2026-12-02 and needs repo access to fetch) — `github.com/ken-muritu/caspahub-flutter/releases/tag/v0.1.0`, 68MB, matches the Fungu precedent's own release-based distribution exactly. User caught that it was only sitting as an Actions artifact and asked directly — good catch, wasn't proactively done.

**Bottom line for a Nairobi pilot, one paragraph:** the consumer golden path (find a branch → check queue busyness/wait → book or join the virtual queue with real OTP verification → scan a branch QR to check in → M-Pesa STK push → live-tracked wash with a collect QR when ready) is real, connects to the live `caspahub-booking` API with zero mock data, and now has a confirmed-compiling Android release build. What is NOT production-ready: staff/manager mobile screens and saved vehicles (both blocked on NextAuth's account-session cookies having no mobile-friendly equivalent — deliberately not reverse-engineered blind), push notification *delivery* (FCM registration code exists but the backend's `/api/push/subscribe` only knows how to send Web Push, not FCM — needs Firebase Admin SDK added backend-side), the scheduler-dependent reminder notifications from the Step 2 audit (needs Vercel Pro or an external cron), and the 30-day account soft-delete. Every one of these is a named, reasoned gap in the repo's own README or the gap-audit doc — nothing was silently left half-built while being called done.
