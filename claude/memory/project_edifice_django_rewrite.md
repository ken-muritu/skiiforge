---
name: project-edifice-django-rewrite
description: "Django full-parity rewrite of Edifice — repo at ken-muritu/edifice-django (private), phases 0-3 done, coordination rules"
metadata: 
  node_type: memory
  type: project
  originSessionId: 87a527cc-f23c-4363-b02e-2293642860e7
  modified: 2026-08-27T15:48:26.097Z
---

Edifice → Django full-parity rewrite — **LIVE at https://edifice-django.vercel.app** (own Vercel project; owner reversed the initial cutover same-day: edificesite.vercel.app went back to the Next.js app via `vercel redeploy` of build c8kwwar2w — free plan only allows one-step rollback, redeploy-from-source was the workaround). Repo: github.com/ken-muritu/edifice-django (private). Infra: Vercel serverless Python (project `edifice-django`; envs incl. DATABASE_URL→Render), production Postgres = Render free tier `edifice-django-prod` (**expires 2026-09-24** — needs basic_256mb upgrade at review), email = Brevo SMTP relay w/ v1 credentials, PayHero + ENCRYPTION_KEY inherited via `vercel env pull`. The `edifice` project's env was restored to its exact v1 set after my temporary additions. Prod DB URL cached at ~/.pg_prod_url (0600); Django secret at ~/.edifice_django_secret.

Done through commit 7a7f965: all 42 tables ported (metadata + tenants + academics + people + facilities + comms incl. message_deliveries unique_together fixing the v1 no-PK dedupe bug); RBAC matrix verbatim; full auth flows (rate-limited, digest tokens); billing services + webhook + subscription_is_active() expiry gate (F7 fix); dashboard with real computed metrics + at-risk flags page (rule stated on-page, boundary test pins strict <80); students CRUD pattern (the template for remaining module views); honest marketing pages with DB-driven pricing; ETL crypto proven against golden vectors from v1's TypeScript.

**FULL PARITY REACHED (2026-08-26, main @ 8356eff, 261+ tests)**: all v1 routes/pages/views ported incl. accept-invite, onboarding wizard (school+branch+super_admin), invite-send w/ digest tokens, renewal STK trigger, admin plan-set, presence heartbeat, compare/how-it-works/privacy/terms pages, robots/sitemap/PWA manifest, security headers + CSP (inline-style tech-debt marked), Sentry optional via SENTRY_DSN, etl_backup JSON snapshotter, etl_verify reconciliation (source=target verified). Product decisions self-settled per owner's no-gates mandate: Tenant.trial_ends_at (14d, set in save() choke point, derive-don't-mutate status via billing.services.tenant_effective_status/trial_expired); Notification/Activity stay tenant-scoped-only. Honesty-parity sweep vs F1/F7/F12/F13 clean (one F7 recurrence in finance list fixed).

Remaining knowns: Render DB expires 2026-09-24; CSP still allows style unsafe-inline (CSS extraction pending); real-data login smoke test needs owner credentials; SSE intentionally absent (poll is primary on serverless).

Gotchas learned: Render CLI-created Postgres defaults to IP-block-all (`--clear-ip-allow-list` keeps blocking! must set cidr=0.0.0.0/0); Vercel's uv builder chokes on config-only pyproject.toml (split into .ruff.toml + pytest.ini); parallel remote agents sharing this box's Postgres cause test-DB collisions (conftest.py now force-cleans stale test db; prefer sequential dispatch).

Visual parity shipped 2026-08-26 late: design tokens ported verbatim from v1 globals.css (indigo #4f46e5 / navy #1a1f5e sidebar / cyan accent, 16px radii, soft shadows, full dark set), Inter typeface, app_shell.html navy sidebar w/ RBAC-filtered nav (core/context_processors.py), public header/footer shell; static served via explicit route (Vercel lambda); CSP extended for Google Fonts. Known visual deltas vs Next remain: no per-module icon set, marketing hero simpler, no theme toggle button (system-follow only).
Stage B finished + Stage D foundation (2026-08-26 night): 34 vendored Lucide icons via {% lucide %} tag; v1 PageHeader partial (page-hero card) on dashboard/students/settings/admin; marketing hero rebuilt on v1 structure w/ honest copy. **Stage D started: inertia-django 2.0 hybrid** — X-Inertia clients get React components (@inertiajs/react 2, Vite 6, committed static/dist since Vercel lambda lacks npm), classic requests keep server templates; login route dual-mode verified live both ways (server "Sign in" page AND JSON component Auth/Login). Rollout pattern established: convert pages one at a time, tests stay green throughout. Route-parity render tests added after merge/stash cycle dropped pages_content.py (caught 500s in prod via sweep, not locally).

**Where Stage D stalled (2026-08-26 night, session `50b6044a` interrupted + ox-alpha session `87a527cc` died on OpenRouter 402):** CRITICAL self-caught bug — React frontend silently never hydrated (only one generic bundle; fix verified via real code-split page chunks Dashboard-*.js). Converted/merged before the stop: rich-app-shell `9c615eb`, billing renewal flow, module-polish `943c757` (icons+KPI tiles on 8 modules), messages-page `0cf6420`, trial-tracking `c76852b`, honesty-parity `8cee130`. **User manually stopped 3 conversion agents mid-flight** (Students page, academic-cluster, alumni/portal+console/account) — those pages remain Django templates; user's explicit demand was full UI identity ("users shouldn't notice they're on different platforms"). Also outstanding at death: shim rename .js→.jsx, a Vite build failure, 3 test failures, pending peer markup-branch merge, 8-section dashboard port, DTO field verification. Hazard: `ParentStudentLink` is not tenant-scoped by its manager (cross-tenant leak risk, handled in one view only). Live bug found in deployed students/templates/students/form.html.